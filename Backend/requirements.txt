# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
psycopg2-binary
alembic==1.13.1
asyncpg==0.31.0

# Vector Database
qdrant-client==1.7.3

# Background processing (transcript ingestion runs on Celery; Redis is the broker)
celery==5.6.3
redis==8.0.0

# AI/ML
openai>=1.109.1   # needs max_completion_tokens support for gpt-5.x models
tiktoken==0.5.2
cohere==7.0.8     # retrieval re-ranker

# Transcript parsing (.docx uploads)
python-docx==1.2.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Authentication (JWT + password hashing)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# CORS
fastapi-cors==0.0.6

# Testing
pytest==7.4.4
httpx==0.26.0
